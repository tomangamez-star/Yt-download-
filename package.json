{
  "name": "yt-downloader-render",
  "version": "1.0.0",
  "private": true,
  "description": "Render-ready YouTube search and download web app powered by yt-dlp and FFmpeg.",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check lib/ytdlp.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.1.0",
    "express-rate-limit": "^8.1.0",
    "helmet": "^8.1.0"
  }
}
